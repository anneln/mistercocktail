 <h1>Listing cocktails</h1> <%= link_to 'New cocktail', new_cocktail_path %>

<table>
  <thead>
    <tr>
      <th>name</th>
      <th></th>

      <th colspan="3"
  </thead>

  <tbody>
    <% @cocktails.each do |cocktail| %>
      <tr>
        <td><%= link_to cocktail.name, cocktail_path(cocktail) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

